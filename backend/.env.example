# ⚠️ IMPORTANT: This file is for LOCAL DEVELOPMENT ONLY (without Docker)
# 
# If using Docker Compose (recommended):
#   - DO NOT copy this file to .env in the backend directory
#   - Instead, configure environment variables in the root .env file
#   - Docker Compose will pass those variables to the backend container
#
# If running backend locally (without Docker):
#   - Copy this file: cp .env.example .env
#   - Update DB_HOST to localhost and CORS_ORIGIN to http://localhost:3000
#
# The values below are configured for Docker Compose deployment.

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# For Docker: Use DB_HOST=postgres (container name)
# For local development without Docker: Use DB_HOST=localhost
DB_HOST=postgres
DB_PORT=5432
DB_NAME=gmao_db
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# CORS Configuration
# For Docker: Frontend runs on port 3010
# For local development: Use port 3000
CORS_ORIGIN=http://localhost:3010

# Ollama Configuration
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama2
OLLAMA_TIMEOUT=30000

# Whisper Configuration
WHISPER_URL=http://localhost:8000
WHISPER_TIMEOUT=300000
